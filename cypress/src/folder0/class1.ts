/**
 * Some code blocks
 *
 * empty since tag
 *
 * @since
 */
export class Class1{

    /**
     *
     *
     * Javascript:
     * ```javascript
     *      console.log("hello world!") // a comment
     *      if(this.color === "blue"){
     *          do.something()
     *      }
     * ```
     */
    javascript:String;

    /**
     *
     *
     * json:
     * ```json
     * {
     *      "compilerOptions": {
     *          "target": "es5"
     *       }
     *  }
     * ```
     */
    json:String;

    /**
     * code fragments should not include double newLines
     *
     * css:
     * ```css
     *  :root {
     * --var1: 20%;
     * --var2: rgba(0,0,0,0.95)
     * }
     * body {
     *  color: var(--var2);
     * background-color: whitesmoke;
     * }
     * ```
     */
    css:String;

    /**
     * No language specified:
     * ```
     * $ fortune | cowsay
     *   ______________________________________
     *   / You like to form new friendships and \
     *   \ make new acquaintances.              /
     *   --------------------------------------
     *           \   ^__^
     *           \  (oo)\_______
     *               (__)\       )\/\
     *                   ||----w |
     *                   ||     ||
     * ```
     *
     * <code class="custom-code">Some inline code</code>
     */
    noLanguage:String;
}