import React from 'react'
import { graphql, useStaticQuery, Link } from 'gatsby'
import { pathToModule, fixModuleName } from "../util/util"

export default (props) => {
 

  return (
    <div>
    <h1>name: </h1>
      <ul>
        ul
      </ul>
    </div>
  )
}